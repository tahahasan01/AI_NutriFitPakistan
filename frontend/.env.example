# URL of the FastAPI backend that Next.js proxies /api/* to.
# Local dev:
BACKEND_URL=http://localhost:5000
# Production (set in Vercel project env):
# BACKEND_URL=https://your-fastapi-host.example.com
